extends layout

block content
  h1= title

  hr
  each game in genreGames
    li
      a(href=game.url) #{game.title}
      p #{game.summary}

  if genreGames.length > 0
    hr
  p
    a(href=genre._id + '/update') Update Genre
  p
    a(href=genre._id + '/delete') Delete Genre
